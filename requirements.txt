flask>=3.0.0
twilio>=7.0.0
python-dotenv>=1.0.0
pystray>=0.19.0
pillow>=10.0.0
cryptography>=46.0.0
# Install twilio with: pip install --only-binary :all: twilio
# (aiohttp dependency requires pre-built wheel on ARM/no C++ compiler)
